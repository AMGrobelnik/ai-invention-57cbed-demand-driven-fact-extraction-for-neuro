# RFDE Rigorous Evaluation: Predicate Verification, McNemar Tests, ECE

`demo/` — Self-contained demo (Colab-ready notebook or markdown). Run without setup.  
`src/` — Full source code, data, and outputs from the experiment execution.

**Type:** evaluation  
**ID:** `art_vKL3deD0Yhq6`

## Layman Summary

Measures how often each AI reasoning method makes unsupported factual claims, tests whether performance gaps are statistically real, and checks if RFDE's advantage holds across task types.

## Full Summary

Rigorous re-evaluation of the RFDE neuro-symbolic pipeline vs. CoT, RAG, and LINC baselines on 26 tasks (8 synthetic, 8 CLUTRR-style family relations, 10 RuleTaker-style deductive). Fixes all three measurement flaws from iteration-1.

**Metrics computed (8 total):**

1. **Independent Predicate Verification (PRIMARY)**: Hallucination rate computed via document-span regex matching independent of answer accuracy. RFDE: 12.5% (3/24 asserted predicates lacked direct doc-span evidence — triggered by over-eager confidence=1.0 on derived goals); CoT: 0.0%; RAG: 0.0%; LINC: 0.0%. Note: iteration-1 reported 0.0% for RFDE because it derived hallucination from error_rate, not from independent document grounding — this is the corrected, methodologically sound measurement.

2. **Per-Class Accuracy**: All methods report yes/no breakdown separately. RFDE: yes=1.000, no=1.000. CoT: yes=0.958, no=1.000. RAG: yes=0.417, no=1.000. LINC: yes=0.833, no=1.000. The 92.3% majority-class imbalance (24 yes / 2 no) is flagged as a protocol violation.

3. **Atomic Extraction Precision/Recall**: RFDE leaf-predicate extraction achieves precision=1.000, recall=1.000, F1=1.000 on the 26-task set.

4. **McNemar's Paired Tests** (binomial exact, two-sided): RFDE vs CoT: p=1.000 (not significant, only 1 discordant pair). RFDE vs RAG: p=0.0001 (significant, effect=1.000). RFDE vs LINC: p=0.125 (not significant at α=0.05, only 4 discordant pairs).

5. **Expected Calibration Error (ECE)**: RFDE ECE=0.077 (confidence overestimates accuracy in the 0.0 bin for negative examples). CoT/RAG/LINC: N/A (no confidence scores reported).

6. **Decomposition Type Stratification**: Tasks classified as atomic_only (n=15), mixed (n=6), derived_required (n=5). RFDE achieves 100% accuracy across all strata. Per-stratum hallucination rates also computed.

7. **Inference Latency**: RFDE mean=1.523s, median=1.347s, p95=3.244s per task (all latency from LLM calls; no baseline latency available from experiment data).

8. **Protocol Violation Flags**: Dataset flagged as imbalanced (92.3% yes-class) and undersized (n=26 < 200). All results should be interpreted with caution.

**Key findings**: RFDE achieves perfect answer accuracy across all decomposition types. However, independent predicate verification reveals 12.5% hallucination rate for RFDE when measuring at the predicate level (vs 0% when measuring answer accuracy), suggesting that demand-driven grounding does not fully eliminate unsupported assertions. Statistical significance vs CoT and LINC is not established (too few discordant pairs at n=26). Only the RFDE vs RAG gap is significant.

**Output files**: eval.py (evaluation script), full_eval_out.json (complete, 63KB), mini_eval_out.json (9-example subset), preview_eval_out.json (truncated preview). Schema validated against exp_eval_sol_out. Dependencies pinned in pyproject.toml (loguru==0.7.3, numpy==2.4.6, scipy==1.17.1).

## Dependencies

- `art_2ky8i9inuKzN` — ground truth labels
- `art_of5nbNJ5eTgQ` — baseline implementations

## Output Files

- `eval.py`
- `full_eval_out.json`
- `mini_eval_out.json`
- `preview_eval_out.json`

## Demo Files

- **eval.py** — Evaluation script with metrics computation

---
*Generated by AI Inventor Pipeline*
